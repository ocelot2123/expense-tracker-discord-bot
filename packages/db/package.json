{
  "name": "db",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "dev": "prisma studio --port 5556",
    "with-env": "dotenv -e ../../.env --",
    "start": "prisma studio --port 5556"
  },
  "dependencies": {
    "@prisma/client": "^4.13.0"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "prisma": "^4.13.0",
    "typescript": "^5.0.4"
  }
}
